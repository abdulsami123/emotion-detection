__pycache__/
*.pyc
.pytest_cache/

# ---------------------------------------------------------------------------
# Confidential trial assets. NEVER commit these.
# The trial brief section 5 requires production-call audio be treated as
# confidential and not uploaded to public services. The target GitHub repo is
# public. Place the provided files in reference/ locally to run the tests:
#   reference/call_001.ogg, call_002.ogg, call_003.ogg, labels.csv
# ---------------------------------------------------------------------------
reference/
aa/
# Transcripts derived from the confidential calls - same category as the audio.
tests/fixtures/asr_baseline.json

# local model cache and batch scratch
models/
_batches/
