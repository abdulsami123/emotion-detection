# Pinned to the versions this system was built and measured against.
#
# The gradio / transformers / pydantic triple is genuinely constrained - do not
# relax these three without re-testing the dashboard AND the ML stack together:
#
#   gradio >= 6.0   pulls huggingface-hub >= 1.0, which transformers 4.46
#                   rejects (requires >=0.23.2,<1.0). Importing autoace.pipeline
#                   then fails outright.
#   gradio 4.44 /   work with the ML stack but return HTTP 500 on page load
#   gradio 5.9      under pydantic >= 2.12 (TypeError in gradio_client.utils
#                   .get_type on an `additionalProperties: true` shorthand).
#                   Reproduced with a trivial one-widget app, so it is an
#                   environment incompatibility, not an application bug.
#   gradio 5.44.1   pins pydantic < 2.12 and huggingface-hub < 1.0, satisfying
#                   both sides. This is the only combination verified working.

numpy>=1.26
scipy>=1.11
librosa>=0.10
soundfile>=0.12
torch>=2.13
torchaudio>=2.11
transformers>=4.46,<4.47
huggingface-hub>=0.33.5,<1.0
faster-whisper>=1.0.3
opensmile>=2.5
speechbrain>=1.0
scikit-learn>=1.5
silero-vad>=5.1
openai>=1.50            # tone classifier (see autoace/config.py LLM_MODEL)
pydantic>=2.5,<2.12
gradio==5.44.1
pandas>=2.1
pytest>=8.0
